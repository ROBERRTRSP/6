{
  "installCommand": "npm install",
  "buildCommand": "npm run build && if [ -n \"$VERCEL\" ] && [ \"$VERCEL_ENV\" != \"development\" ]; then rm -rf public && mkdir -p public && cp -r 6/client/dist/. public/; fi",
  "rewrites": [
    {
      "source": "/((?!api/).*)",
      "destination": "/index.html"
    }
  ]
}
